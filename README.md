**Live Demo:** [https://Alexandr-Musihin.github.io/api-test-project/report.html](ссылка)
**CI/CD Status:** ![Tests](https://github.com/api-test-project/.github/workflows/tests.yml/badge.svg

# 🐾 Petstore API Test Automation Framework

![Tests](https://img.shields.io/badge/tests-14%20passed-brightgreen)
![Python](https://img.shields.io/badge/python-3.9+-blue)
![Status](https://img.shields.io/badge/status-complete-success)

Полный фреймворк для автоматизированного тестирования Petstore API (https://petstore.swagger.io/) с поддержкой 3 уровней сложности тестов.

## 📋 Оглавление

- [Особенности](#особенности)
- [Структура проекта](#структура-проекта)
- [Установка](#установка)
- [Запуск тестов](#запуск-тестов)
- [Уровни тестирования](#уровни-тестирования)
- [Отчеты](#отчеты)
- [CI/CD](#cicd)

## ✨ Особенности

- ✅ **14 готовых тестов** с 100% успешностью
- 📊 **3 уровня сложности** (базовый, продвинутый, профессиональный)
- 🚀 **Параллельное выполнение** тестов
- 📈 **Подробные отчеты** (Allure, HTML)
- 🔧 **Валидация JSON схем**
- 🐳 **Docker поддержка**
- ⚡ **CI/CD готовность**

## 📁 Структура проекта

```
petstore-api-tests/
├── tests/                    # Тестовые сценарии
│   ├── test_level1_basic.py      # Базовые CRUD операции (4 теста)
│   ├── test_level2_advanced.py   # Продвинутые сценарии (6 тестов)
│   └── test_level3_professional.py # Профессиональные тесты (4 теста)
├── utils/                    # Вспомогательные утилиты
│   ├── api_client.py        # HTTP клиент для работы с API
│   ├── validators.py        # Валидаторы JSON ответов
│   └── helpers.py           # Генераторы тестовых данных
├── data/                    # Тестовые данные
│   ├── test_data.json       # Наборы тестовых данных
│   └── schemas/
│       └── pet_schema.json  # JSON схемы для валидации
├── requirements.txt         # Зависимости Python
├── pytest.ini              # Конфигурация pytest
└── README.md               # Документация
```

## 🚀 Быстрая установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd petstore-api-tests
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Проверка установки
```bash
pytest --version
```

## 🧪 Запуск тестов

### Запуск всех тестов
```bash
pytest
```

### Запуск по уровням сложности
```bash
# Уровень 1: Базовые тесты
pytest tests/test_level1_basic.py -v

# Уровень 2: Продвинутые тесты
pytest tests/test_level2_advanced.py -v

# Уровень 3: Профессиональные тесты
pytest tests/test_level3_professional.py -v
```

### Запуск с отчетами
```bash
# HTML отчет
pytest --html=report.html --self-contained-html

# Allure отчет
pytest --alluredir=allure-results
allure generate allure-results -o allure-report --clean
allure open allure-report
```

### Параллельный запуск
```bash
pytest -n 4  # 4 параллельных потока
```

## 📊 Уровни тестирования

### Уровень 1: Базовый (4 теста)
- ✅ Создание питомца (POST /pet)
- ✅ Получение питомца по ID (GET /pet/{petId})
- ✅ Обновление питомца (PUT /pet)
- ✅ Удаление питомца (DELETE /pet/{petId})

### Уровень 2: Продвинутый (6 тестов)
- ✅ Негативное тестирование с невалидными данными
- ✅ Поиск питомцев по статусам (available, pending, sold)
- ✅ Загрузка изображения для питомца
- ✅ Обработка несуществующих ресурсов

### Уровень 3: Профессиональный (4 тестов)
- ✅ Валидация JSON схем ответов
- ✅ Сложные сценарии с зависимостями
- ✅ Работа с HTTP заголовками
- ✅ Параметризация через внешние файлы данных

## 📈 Отчеты

Проект поддерживает несколько форматов отчетов:

### 1. HTML отчет
```bash
pytest --html=report.html --self-contained-html
# Открыть: report.html
```

### 2. Allure отчет (рекомендуется)
```bash
# Создать директорию для результатов
mkdir -p allure-results allure-report

# Запустить тесты
pytest --alluredir=allure-results

# Сгенерировать отчет
allure generate allure-results -o allure-report --clean

# Открыть отчет
allure open allure-report
```

### 3. Консольный отчет
```bash
pytest -v  # Подробный вывод
pytest -q  # Минималистичный вывод
```

## 🐳 Docker поддержка

### Сборка и запуск в Docker
```bash
# Собрать Docker образ
docker build -t petstore-tests .

# Запустить тесты в контейнере
docker run --rm petstore-tests
```

## 🔄 CI/CD интеграция

Проект готов для интеграции с популярными CI/CD системами:

### GitHub Actions
```yaml
# .github/workflows/tests.yml
name: API Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
      - run: pip install -r requirements.txt
      - run: pytest --alluredir=allure-results
```

## 🛠️ Технологический стек

- **Python 3.9+** - основной язык
- **pytest** - фреймворк тестирования
- **requests** - HTTP клиент
- **jsonschema** - валидация JSON
- **Allure** - генерация отчетов
- **pytest-html** - HTML отчеты
- **pytest-xdist** - параллельное выполнение

## 🎯 Пример теста

```python
def test_create_pet(self, api_client, pet_data):
    """Тест создания питомца"""
    response = api_client.create_pet(pet_data)
    
    assert response.status_code == 200
    response_data = response.json()
    assert response_data['id'] == pet_data['id']
    assert response_data['name'] == pet_data['name']
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Добавьте тесты
4. Запустите существующие тесты
5. Создайте Pull Request

## 📞 Поддержка

Для вопросов и предложений:
- Создайте Issue в репозитории
- Проверьте существующие Issues перед созданием нового

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

⭐ **Если проект был полезен, поставьте звезду на GitHub!**

